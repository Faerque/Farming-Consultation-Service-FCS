import React from 'react';

const Loading = () => {
    return (
        <section>
            <progress className="bg-success progress w-56"></progress>
        </section>
    );
};

export default Loading;